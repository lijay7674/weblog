server:
  port: 8082

spring:
  application:
    name: forum-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: forum-dev
      config:
        server-addr: localhost:8848
        namespace: forum-dev
        file-extension: yaml
