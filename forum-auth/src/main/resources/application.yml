server:
  port: 8081

spring:
  application:
    name: forum-auth
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: forum-dev
      config:
        server-addr: localhost:8848
        namespace: forum-dev
        file-extension: yaml
