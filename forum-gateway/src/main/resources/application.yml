server:
  port: 8080

spring:
  application:
    name: forum-gateway
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: forum-dev
      config:
        server-addr: localhost:8848
        namespace: forum-dev
        file-extension: yaml
